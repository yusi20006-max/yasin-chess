# Android Performance Baseline

The CI benchmark records:

- chess-core move generation throughput;
- production web bundle size;
- reproducible Android debug APK generation.

Device-specific cold-start and frame-time measurements remain emulator/device observations rather than claims derived from the JavaScript benchmark.
