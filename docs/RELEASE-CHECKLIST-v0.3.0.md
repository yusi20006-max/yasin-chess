# v0.3.0 Release Checklist

1. Merge #238 to `main`.
2. Verify `package.json` reports `0.3.0`.
3. Create and push tag `v0.3.0`.
4. Confirm the release workflow completes.
5. Verify SHA256SUMS and Android artifacts.
6. Install the signed production APK on a clean Android device and run the offline smoke test.
