# Mobile performance baseline

Run `npm run benchmark:mobile` after a production build. The harness measures deterministic move-generation/playback throughput and records total and per-ply latency. Device-specific results should be recorded from representative Android hardware; the repository does not fabricate a device baseline.
