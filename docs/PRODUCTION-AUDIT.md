# Production audit

Required gates: reproducible `npm run build`, self-contained `dist/` assets, valid manifest/start URL/display mode, and the offline smoke test. Deployment must serve the generated asset paths without rewriting them to network-only endpoints.
