import {useEffect,useState} from 'react';import type {ClockState} from '../app/clock';
export default function ChessClock({state}:{state:ClockState}){const[,setNow]=useState(Date.now());useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),250);return()=>clearInterval(id)},[]);const fmt=(ms:number)=>`${Math.floor(ms/60000)}:${String(Math.floor(ms/1000)%60).padStart(2,'0')}`;return <div className="chess-clock" role="timer"><span className={state.turn==='w'?'active':''}>{fmt(state.whiteMs)}</span><span className={state.turn==='b'?'active':''}>{fmt(state.blackMs)}</span></div>}
