# Accessibility notes

- The board uses native buttons so every square is keyboard focusable.
- Square names, occupancy, selection, and legal-move state are exposed through accessible names.
- Game status and AI thinking use live regions.
- Motion respects `prefers-reduced-motion`.
- Drag interaction remains an enhancement; click/tap remains the primary accessible move path.
