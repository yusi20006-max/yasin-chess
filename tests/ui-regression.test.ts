import {describe,expect,it} from 'vitest';import {loadSettings,saveSettings} from '../src/app/settings';import {setLocale,t} from '../src/i18n';
describe('UI regression contracts',()=>{it('settings survive reload boundary',()=>{localStorage.clear();saveSettings({theme:'dark',language:'en'});expect(loadSettings().theme).toBe('dark');expect(loadSettings().language).toBe('en')});it('locale updates text and direction',()=>{setLocale('fa');expect(t('newGame')).toBe('بازی جدید');expect(document.documentElement.dir).toBe('rtl');setLocale('en');expect(t('newGame')).toBe('New Game');expect(document.documentElement.dir).toBe('ltr')})});