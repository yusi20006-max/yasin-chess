import {describe,expect,it} from 'vitest';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const read=(p:string)=>fs.readFileSync(path.join(root,p),'utf8');
describe('PWA contracts',()=>{it('has manifest and service worker registration',()=>{expect(fs.existsSync(path.join(root,'public/manifest.webmanifest'))).toBe(true);expect(fs.existsSync(path.join(root,'src/pwa/registerSW.ts'))).toBe(true)});it('service worker has cache strategy',()=>{const sw=read('public/sw.js');expect(sw).toMatch(/cache|Cache|fetch/)});it('manifest is installable',()=>{const m=JSON.parse(read('public/manifest.webmanifest'));expect(m.name).toBeTruthy();expect(m.start_url).toBeTruthy();expect(m.display).toMatch(/standalone|minimal-ui/)});});