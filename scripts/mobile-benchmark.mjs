import {performance} from 'node:perf_hooks';import {ChessGame} from '../dist/core/game.js';const rounds=20;const start=performance.now();let plies=0;for(let i=0;i<rounds;i++){const g=new ChessGame();for(let j=0;j<20&&g.status()==='playing';j++){const ms=g.moves();if(!ms.length)break;g.play(ms[0]);plies++}}const ms=performance.now()-start;const report={rounds,plies,totalMs:Number(ms.toFixed(2)),avgMsPerPly:Number((ms/Math.max(1,plies)).toFixed(3))};console.log(JSON.stringify(report,null,2));if(!Number.isFinite(report.avgMsPerPly))process.exit(1);